import { Metadata } from 'next';

export const metadata: Metadata = {
  title: 'Simulasi Membaca',
};

const ReadingSimulationPage = () => {
  return <section>Reading Simulation</section>;
};

export default ReadingSimulationPage;
